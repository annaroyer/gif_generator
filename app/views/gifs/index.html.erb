<h1>Gifs</h1>
<% @gifs.each do |gif| %>
  <img src=<%= gif.image_path %> alt=<%= gif.category %>>
  <p><%= gif.category %></p>
  <%= form_tag favorites_path do %>
    <%= hidden_field_tag :gif_id, gif.id %>
    <%= submit_tag 'favorite' %>
  <% end %>
<% end %>
